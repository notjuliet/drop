{
  "name": "drop",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "cd web && bun run dev & bun run --hot src/index.ts",
    "build": "cd web && bun install && bun run build",
    "start": "bun run build && bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.12.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
